# 我花了一晚上，用 AI 搓了个链上大户透视镜

故事是这样的。

Virtuals Protocol，Base 链上的 AI Agent 代币发射平台。Season 1 的时候我进去玩了一下，小额试了几个 Top 代币，发现进前十也没啥流量，前三又卷不进去，前四的分数几乎一样。

算了，润了。

然后我就没再看了。

没想到 Season 2 改了机制。取消了订阅制，把 10 万跟投收益的 50% 拿来回购销毁代币，不再奖励给订阅者了。什么意思呢，流动性留在了 Virtuals 平台上，会推高代币价格，带来更多流动性。飞轮效应。

然后有个代币直接起飞了，市值 5 万刀，未实现收益 2 万刀。如果一半拿来回购，这价格得往上顶。群里 efo 老师提了一嘴，当时价格 $0.00005。efo 老师自己买了将近 2K 刀。

按打新比例，我的仓位是老师的 1/20，应该买 100 刀。

我没买。

后来价格到了 $0.00009，差不多翻倍了。

我还是没买。

现在价格跌到了 $0.000073，比高点回撤了不少，但比 efo 老师当时喊的 $0.00005 还是贵了一些。

怎么办？

冲动上车之前，我想搞清楚一件事，大户们的成本到底是多少。他们是在什么价位进的？现在是浮盈还是浮亏？这个价位到底是高是低，光看 K 线看不出来，得看大户的筹码分布。

这个信息，链上全都有，但没有现成工具帮你算好。

那就自己搓一个。

说实话，写这种教程类的东西，我一直是有心理负担的。因为我自己也不是什么技术大佬，也不会写代码，凭什么教别人呢？但这次搓完之后，我觉得这个过程本身就挺有意思的，不是教你怎么写代码，而是聊聊一个完全不懂代码的人，怎么用 AI 把一个想法变成一个能跑的工具。

可能有些想法还不成熟，但我已经毫无保留的分享了。

---

坦率的讲，很多人 vibe coding 的第一步是打开 Claude 或者 Cursor，然后说「帮我做一个 xxx」。

我觉得这是错的。

你得先想清楚你要什么。AI 是你的手，不是你的脑子。你不想清楚，它就会帮你做一个看起来对但其实不是你要的东西，然后你改来改去浪费一晚上。

我在打开 AI 之前，先想了三个问题。

第一个，我要看到什么。

一个网页，上面有一个大户排行榜，前 20 名就够了。每个人我想知道，花了多少钱买的，手里还有多少代币，他的平均买入价对应什么市值，现在是赚是亏，已经卖掉的部分赚了多少，最后一次操作是什么时候。

第二个，数据从哪来。

我知道一个基本事实，链上所有转账都是公开的。每一笔代币转账，区块链上都有记录。只要把这个代币从创建到现在的所有转账记录拉下来，就能还原出谁买了多少、谁卖了多少、谁转给了谁。

第三个，成本怎么算。

这个我想了一会儿。假设你分三次买入，第一次 100 个代币花了 $10，第二次 200 个花了 $30，第三次 100 个花了 $20。总共 400 个，花了 $60，均价 $0.15。

如果你卖掉 200 个，收到 $40，那卖出占总持仓的 50%，这 50% 的成本就是 $60 乘以 50% 等于 $30，已实现收益就是 $40 减 $30 等于 $10。剩下 200 个的成本还是 $30。

这叫加权平均成本法。我不需要知道这个名词，但我得把这个逻辑想清楚，才能告诉 AI 怎么算。

想清楚这三个问题，才打开 AI。

---

说到跟 AI 对话这块，我跟它说的第一句话大概是这样的。

「我想做一个工具，分析 Base 链上某个代币的所有持有者的买入成本。需要扫描链上所有转账记录，区分买入卖出和普通转账，计算每个人的成本和收益。最后用一个网页展示排行榜。帮我设计一下怎么做。」

注意，我没有说用什么语言、什么框架、什么数据库。我不知道，也不需要知道。AI 会根据需求帮我选。

它选了什么我不太关心，我关心的是它的方案能不能跑起来。它告诉我需要装一些东西，我就照着装。它帮我创建了项目目录和一堆文件，我也不需要看懂每一行。

但有一个东西我得自己搞，RPC 节点。

AI 告诉我，要读取链上数据需要一个叫 RPC 节点的东西，相当于跟区块链通信的入口。有免费的公共节点，但速度慢、限制多。它建议我去 Alchemy 注册一个免费账号拿 API Key。

我就去注册了。免费的，填个邮箱就行。拿到一个链接，填到配置文件里。

另外还可以选配一个 CoinGecko 的 API Key，用来查代币的历史美元价格。也是免费的。

---

回到扫描链上数据这块。

AI 需要知道我要分析哪个代币。我其实只知道一个东西，代币合约地址，在 Virtuals 官网上代币页面就能直接看到。

我把地址丢给 AI，跟它说，这是代币地址，在 Base 链上，是 Virtuals Protocol 平台发的代币，帮我从这个代币创建开始，扫描到最新区块，把所有转账记录都拉下来。

剩下的事情 AI 自己搞定了。它通过代币地址去链上查到了部署区块号，也找到了交易对合约地址。这些我都不需要自己去查。

AI 还告诉我，一次性扫描几百万个区块会被 RPC 节点拒绝，需要分批查。而且查太快会被限流。它帮我写了自动分批、自动重试的逻辑。

这些我不需要操心，但我需要知道一件事，第一次跑会比较慢，几分钟，因为要扫描从代币创建到现在的所有区块。之后再跑就快了，因为只需要扫描上次停下来之后的新区块。

---

顺着上面的再聊聊交易分类。

链上的转账记录只告诉你「A 给 B 转了 100 个代币」，不会告诉你这是买入还是卖出。

但我知道一个规律。如果代币是从交易对池子转到某个人的钱包，这个人就是买入了。如果代币是从某个人的钱包转到交易对池子，这个人就是卖出了。其他情况就是普通的钱包间转账。

我把这个逻辑告诉 AI，帮我分类每笔转账。

这块需要注意一下，转账记录只有代币数量，没有花了多少钱。但我知道 Virtuals 平台的交易机制，买代币的时候你付的是 VIRTUAL 代币，其中 1% 是平台税。同一笔交易里，VIRTUAL 的转账记录也在链上。

我跟 AI 说，买入的时候用户支付 VIRTUAL 代币，其中 1% 作为税费，帮我从同一笔交易的记录里找到 VIRTUAL 的转账，反推出用户实际付了多少 VIRTUAL，然后再查 VIRTUAL 当时的美元价格，就能算出花了多少美元。

这一步 AI 可能需要你多解释几次。你得把平台的交易机制讲清楚，它才能写对。

---

然后就是算成本了。

这一步我跟 AI 说得很详细，因为这是核心逻辑，算错了整个工具就废了。

用加权平均成本法计算每个持有者的成本。买入就累加持仓数量和总花费。卖出就按卖出比例扣减总花费，已实现收益等于卖出收入减去扣减的花费。转账的话，如果 A 转给 B，B 的成本要继承 A 的均价。

转账继承成本这个点很重要，我是自己想到的。如果不处理这个，大户把币转到小号，小号成本就变成 0 了，数据就不准了。

你想想看，一个大户有 1000 个代币花了 100 刀，转了 500 个给另一个钱包，那另一个钱包的成本应该是 50 刀才对。不然你看到排行榜上一个地址持有一大堆代币但成本是 0，那数据就没有参考价值了。

---

说到前端这块，我跟 AI 说的大概是这样的。

帮我做一个网页 Dashboard，暗色主题。上面要有四个数字卡片，活跃持有者数、当前代币价格、总买入笔数、总卖出笔数。一个大户排行榜，只要前 20 名，表头用中文，钱包地址、花费 USD、累计代币万、买入市值万 USD、未实现收益、已实现收益、更新时间。

钱包地址旁边要有个编辑按钮，点了可以给地址起名字，比如「鲸鱼 A」「efo 老师」，名字要保存下来刷新不丢。赚钱的数字显示绿色，亏钱的显示红色。再来一个饼图展示前 10 名的持仓占比，一个柱状图按天展示买入和卖出的金额。点击地址可以进入详情页看这个地址的所有交易记录。

几个细节我特别强调了。代币数量用「万」做单位，原始数字太大了没有意义。买入市值等于平均买入价乘以 10 亿总供应量，这个数字告诉你这个人是在什么市值的时候进场的。钱包命名要持久化，不能刷新就没了。

AI 帮我搭了一个还挺好看的 Dashboard。这一步基本不需要我操心，AI 对前端 UI 很擅长。

---

到这里工具已经能用了。跑一次命令，扫描数据，打开网页看结果。

但每次有新交易都要手动重跑。

太蠢了。

我跟 AI 说，我想要实时更新，链上有新交易的时候自动检测到，自动更新数据，网页上自动刷新，不需要我手动操作。

AI 给我设计了一个方案。一个常驻的后台进程，每 4 秒检查一次链上有没有新的转账。发现新转账就自动分类、自动算成本、自动更新数据。网页通过一个推送通道，检测到数据更新了就自动刷新。

听起来很美好。

但这一步踩了三个大坑。

我是真的被折腾到了。

---

第一个坑。AI 一开始用了一个叫 watchEvent 的方法来监听链上事件。看起来很优雅，但跑起来直接报错，「filter not found」。

我把错误信息贴给 AI，它试了几种修复方式都不行。我自己去查了一下，发现这个方法底层用了一个叫 eth_newFilter 的接口，但 Base 链的 RPC 节点不支持这个接口。

我跟 AI 说，别用 watchEvent 了，这个方法在 Base 链上不工作。换成最笨的办法，每 4 秒查一次最新区块，看看有没有新的转账记录。

换了之后就好了。

有时候最笨的办法就是最靠谱的办法。

第二个坑。后台进程已经在正常检测新交易了，数据也更新了，但网页上的数字纹丝不动。

。。。

AI 一开始的方案是用一个叫 router.refresh 的方法来刷新页面。但不管怎么调都不生效。我让 AI 查了一下原因，发现是框架的缓存机制在作怪，它觉得数据没变，就不重新读取文件了。

我说，别走框架的刷新机制了，直接让网页自己去请求最新数据。

AI 改成了后台数据更新后发一个信号，网页收到信号后直接调接口拿最新数据。绕过了缓存问题。

第三个坑。有一次我把后台进程关了再重启，发现中间那段时间的交易被漏掉了。

原因很简单，进程重启后它从当前区块开始监控，但中间停机的那些区块里可能有交易。

我跟 AI 说，每次启动的时候先检查上次扫描到哪个区块了，把中间遗漏的区块补扫一遍，然后再开始实时监控。

加了这个追赶逻辑之后，就不怕重启丢数据了。

---

最后跑起来的时候，两个终端窗口，一个跑后台监控进程，一个跑网页服务。打开浏览器，绿色的 Live 灯在闪，大户排行榜一目了然。有新交易发生时数据自动更新。点钱包地址旁边的编辑按钮可以给大户起名字。

我当时就愣住了。

不是因为技术多牛逼，是因为我一行代码都没写，但这玩意真的跑起来了。

---

说真的，这三个坑有一个共同点。AI 给的方案在理论上都是对的，但在特定环境下不工作。RPC 节点不支持某个接口、框架有缓存机制、进程重启会丢状态，这些是你需要理解的「环境知识」。

AI 不知道你的 RPC 节点支持什么不支持什么。AI 不知道你用的框架版本有什么缓存行为。AI 不知道你的进程会不会挂掉重启。

这些东西，你得自己去发现，然后告诉 AI。

这话听着有点刺耳，但我有时候觉得，vibe coding 最难的部分不是让 AI 写代码，而是在 AI 写错的时候，你能不能搞清楚为什么错了。

如果你不理解原因，AI 可能会在错误的方向上越走越远，给你一个更复杂但同样不工作的方案。然后你们就在一个死胡同里反复横跳。

---

回到整个过程这块，我觉得有几个点还是挺重要的。

你不需要会写代码，但你需要会思考。AI 写代码的能力已经很强了，但它不知道你要什么，不知道你的环境有什么限制，不知道哪些边界情况需要处理。你的价值在于定义问题、发现问题、纠正方向。

先想清楚再动手。不要上来就说帮我做一个 xxx。先花 10 分钟想清楚，我要什么数据，数据从哪来，怎么处理，怎么展示。想清楚之后跟 AI 的对话效率会高 10 倍。

分步走，每步验证。不要一口气让 AI 把整个项目写完。先能拿到链上数据，跑一下看看输出对不对。再能区分买卖，抽几笔交易手动验证。然后算成本，拿计算器算一个地址的成本对比一下。搭前端，打开网页看看数据对不对。加实时更新，等一笔新交易看网页会不会自动变。每一步验证通过再进入下一步。不然 bug 叠在一起，你根本不知道是哪里出了问题。

出了问题先理解为什么。不要只说换一种方式试试。搞清楚为什么报错，原因清楚了，解决方案自然就有了。

---

这个工具帮我看清了 FAT 代币大户的成本结构。前 20 名里，大部分人的买入均价集中在 $0.00005 到 $0.00007 这个区间，跟现价 $0.000073 基本持平。说明大户们在成本线附近，没有太大的浮盈也没有太大的浮亏，筹码拿得比较稳。

至于要不要上车。

工具给你数据，决策还是你自己的。

我自己的感受是，在这个信息爆炸的时代，能自己动手去链上扒数据、算成本、做判断，比听任何 KOL 喊单都靠谱。哪怕你不会写代码，AI 已经把这个门槛拉到了地板上。

你需要的不是技术能力，是好奇心，和愿意花时间想清楚问题的耐心。

如果你也想复刻这个工具，代码都在 chain-radar 目录下。要分析其他代币，改一下配置文件里的合约地址就行，Virtuals 官网上直接能看到。如果是非 Virtuals 平台的代币，跟 AI 说清楚新平台的交易机制，让它帮你调整分类逻辑。

大时代啊，朋友们。

以上，既然看到这里了，如果觉得不错，随手点个赞、在看、转发三连吧，如果想第一时间收到推送，也可以给我个星标⭐～

谢谢你看我的文章，我们，下次再见。

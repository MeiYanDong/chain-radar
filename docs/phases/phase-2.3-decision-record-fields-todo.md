# Phase 2.3 Todo：决策记录字段定义

> 维护规则：本文档记录 Phase 2.3 的执行事项。完成字段、文档或实现改动后，Agent 需要更新勾选状态和简短备注。

## 1. 字段冻结

- [x] 明确字段设计原则：必要、无歧义、分类互斥、分类完整。
- [x] 拆分系统自动字段、用户手动字段和系统后验字段。
- [x] 删除第一版自由文本理由，避免歧义过大。
- [x] 将“心态”改为 `primary_psychological_noise`，只记录最可能扭曲判断的主要心理因素。
- [x] 将“信心”改为 `evidence_confidence`，定义为概率估计的证据可靠度。
- [x] 将仓位记录设为条件字段，只有发生交易动作时填写。

## 2. 待讨论

- [x] 确认 `expected_token_direction` 只围绕代币价格方向，而不是 Live P&L 方向。
- [x] 确认 `subjective_probability` 使用 `no_edge = 50`，`up/down = 55 到 95`，按 5 递增，不允许 100。
- [x] 确认 `primary_psychological_noise` 第一版分类足够使用，后续根据真实记录再迭代。
- [x] 确认 `action` 第一版只覆盖现货/多头仓位，不纳入做空、合约和杠杆动作。

## 3. 暂不实现

- [ ] 飞书按钮和私密记录页。
- [ ] 自动补齐 token price 后验结果。
- [ ] 贝叶斯更新视图。
- [ ] Kelly 仓位建议。
